{
  "entities": {
    "user": {
      "title": "User",
      "description": "A user profile in the system.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string"
        },
        "createdAt": {
          "type": "string"
        }
      }
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "user",
      "description": "User profiles."
    }
  }
}
